#For running type "./install.sh"
#If you have problem type "chmod +x install.sh"

sudo apt-get install nasm && echo NASM installed successfully || echo Unable to install NASM

sudo apt install bochs && echo BOCHS installed successfully || echo Unable to install BOCHS